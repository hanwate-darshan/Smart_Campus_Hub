import NotificationList from "@/components/notifications/NotificationList";

export const metadata = {
  title: "Notifications | Teacher Portal",
};

export default function TeacherNotifications() {
  return <NotificationList />;
}
